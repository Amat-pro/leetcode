name: Jira Action
description: jira action
inputs:
  JIRA_BASE_URL_H:
    description: description
    required: true

runs:
  using: 'docker'
  image: 'golang:alpine'
  env:
    JIRA_API_TOKEN: ${{ inputs.JIRA_TOKEN }}
  entrypoint: ["/bin/sh", "-c"]
  args:
    - 'pwd && ls -l'