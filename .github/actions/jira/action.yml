name: Jira Action
description: jira action
inputs:
  JIRA_BASE_URL_H:
    description: description
    required: true

runs:
  using: 'docker'
  image: 'golang:alpine'
  args:
    - '/bin/sh'
    - '-c'
    - 'pwd && ls -l && echo "哈哈哈哈"'